{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.0.3"
  },
  "scripts": {
    "start": "lerna run start && lerna run start --scope wigly-types && node scripts/prep.js",
    "test": "lerna run test"
  }
}
