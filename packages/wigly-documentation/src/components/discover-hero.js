import { h, component } from "wigly";

let Hero = component({
  render() {
    return (
      <div class="discover-hero">
        <h1>Wigly</h1>
        <h2>A silly-small, component-based, JavaScript UI library</h2>
      </div>
    );
  }
});

export default Hero;
