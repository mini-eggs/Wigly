import { h, component } from "wigly";
import Hero from "../components/discover-hero";

let Discover = component({
  render() {
    return (
      <div>
        <Hero />
      </div>
    );
  }
});

export default Discover;
